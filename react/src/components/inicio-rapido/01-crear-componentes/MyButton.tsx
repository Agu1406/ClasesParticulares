/**
 * Botón mínimo de la lección 01 (sin hoja CSS propia; la clase `.boton-tutorial` está en `App.css`).
 *
 * Versión TypeScript equivalente al `.jsx` original para poder importarla desde el agregador TSX.
 *
 * @see https://es.react.dev/learn
 */
export default function MyButton() {
    return <button className="boton-tutorial">Soy un botón</button>;
}
