// La linea llamada "package" podeís ignorarla por completo, mucho más adelante se explica.
package ev1.ut0.ciclovida;

/**
 * En el README hablo sobre esto, el nombre de nuestro archivo es
 * EjemploPrograma.java y cuando escribimos el código tenemos que
 * decirle a Java el nombre del archivo, por eso aquí vemos el
 * mismo nombre sin la extensión (.java).
 * 
 * Este programa esta vacio, por lo tanto, no tiene errores, cuando
 * lo ejecutamos con el compilador de java (javac) este si no detecta
 * ningún problema lo tranforma en un archivo del tipo ".class" el 
 * cual podemos ejecutar en cualquier ordenador que tenga isntalado
 * Java.
 */
public class Ejemplo {
    public static void main(String[] args) {

    }
}
